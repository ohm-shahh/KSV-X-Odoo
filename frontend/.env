# Base URL of the VendorBridge PHP API (served by XAMPP/Apache on port 80).
# Override here if you run the backend elsewhere (e.g. php -S 127.0.0.1:8000).
VITE_API_BASE_URL=http://localhost/vendorbridge-backend/api
